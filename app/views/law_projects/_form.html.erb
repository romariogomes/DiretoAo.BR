<%= form_with(model: law_project, local: true) do |form| %>
  <% if law_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(law_project.errors.count, "error") %> prohibited this law_project from being saved:</h2>

      <ul>
      <% law_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :law_number %>
    <%= form.number_field :law_number, id: :law_project_law_number %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :law_project_description %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :law_project_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
