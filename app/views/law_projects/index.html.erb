<p id="notice"><%= notice %></p>

<div class="container">

<h2>Projetos de lei</h2>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Número</th>
      <th>Autor</th>
      <th>Data</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @law_projects.each do |law_project| %>
      <tr>
        <td><%= law_project.law_number %></td>
        <td><%= law_project.description %></td>
        <td><%= law_project.date %></td>
        <td><%= link_to 'Visualizar', law_project %></td>
        <td><%= link_to 'Editar', edit_law_project_path(law_project) %></td>
        <td>
            <%= button_to law_project, class: 'btn btn-primary', method: :delete, data: { confirm: 'Are you sure?' }  do %>
              <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
            <%end%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>

<%= button_to '+ Novo projeto de lei', new_law_project_path, class: 'btn btn-primary', method: :get  %>
